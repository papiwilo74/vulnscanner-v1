# Bandit configuration for VulnScanner
# Only skips non-cryptographic random jitter used for polite stealth scanning delays
skips:
  - B311  # Pseudo-random generators used for random delay jitter in polite stealth mode

exclude_dirs:
  - tests
  - reports
  - venv
  - .venv
